#include<stdio.h>
int gcd(int n1,int n2);
int main()
{
	int n1,n2,i;
	scanf("%d %d",&n1,&n2);
	int res=gcd(n1,n2);
	printf("GCD = %d",res);
}
int gcd(int n1,int n2)
{
	int i,Gcd;
	for(i=1;i<=n1&&i<=n2;i++)
	{
	    if(n1%i==0&&n2%i==0)
	    Gcd=i;
	    return Gcd;
	}
}
